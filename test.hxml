# Prerequisites: HL/C code should be located in ./test/src-c/

# Windows
# (Environment variable HASHLINKPATH or similar should be set for omitting --hlLibDir)
--cmd haxelib run hlc-compiler --srcDir test\src-c --outDir test\bin --exFile C:\Windows\System32\dbghelp.dll --copyRuntimeFiles --saveCmd test\compile --verbose -w

# Mac
# --cmd haxelib run hlc-compiler --srcDir test/src-c --outDir test/bin --copyRuntimeFiles --verbose --saveCmd test/compile
